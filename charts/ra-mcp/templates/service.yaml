apiVersion: v1
kind: Service
metadata:
  name: {{ include "ra-mcp.fullname" . }}
  labels:
    {{- include "ra-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ra-mcp.selectorLabels" . | nindent 4 }}
