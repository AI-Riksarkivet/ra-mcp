[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
artifacts = ["src/ra_mcp_court_mcp/dist/**"]

[tool.hatch.build.targets.wheel]
packages = ["src/ra_mcp_court_mcp"]

[project]
name = "ra-mcp-court-mcp"
version = "0.11.3"
description = "MCP tools for Swedish court records (Domboksregister & Medelstad) search"
requires-python = ">=3.13"
license = "Apache-2.0"
dependencies = [
    "ra-mcp-court-lib",
    "fastmcp==3.1.1",
]

[tool.uv.sources]
ra-mcp-court-lib = { workspace = true }
