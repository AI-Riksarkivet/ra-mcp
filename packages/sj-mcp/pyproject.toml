[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ra_mcp_sj_mcp"]

[project]
name = "ra-mcp-sj-mcp"
version = "0.11.3"
description = "MCP tools for Swedish State Railways (SJ) records search"
requires-python = ">=3.13"
license = "Apache-2.0"
dependencies = [
    "ra-mcp-sj-lib",
    "fastmcp==3.1.1",
]

[tool.uv.sources]
ra-mcp-sj-lib = { workspace = true }
