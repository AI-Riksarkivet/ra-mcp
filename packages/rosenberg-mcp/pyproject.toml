[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ra_mcp_rosenberg_mcp"]

[project]
name = "ra-mcp-rosenberg-mcp"
version = "0.11.3"
description = "MCP tools for Rosenberg's geographical lexicon of Sweden"
requires-python = ">=3.13"
license = "Apache-2.0"
dependencies = [
    "ra-mcp-rosenberg-lib",
    "fastmcp==3.1.1",
]

[tool.uv.sources]
ra-mcp-rosenberg-lib = { workspace = true }
