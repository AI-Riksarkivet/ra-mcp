[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ra_mcp_filmcensur_mcp"]

[project]
name = "ra-mcp-filmcensur-mcp"
version = "0.11.3"
description = "MCP tools for Swedish film censorship records (Filmcensur) search"
requires-python = ">=3.13"
license = "Apache-2.0"
dependencies = [
    "ra-mcp-filmcensur-lib",
    "fastmcp==3.1.1",
]

[tool.uv.sources]
ra-mcp-filmcensur-lib = { workspace = true }
