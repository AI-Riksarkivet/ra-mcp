[project]
name = "ra-mcp"
version = "0.1.0"
description = "Riksarkivet first MCPs to expose APIs in dataplattformen"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "asyncio>=3.4.3",
    "fastmcp>=2.7.1",
    "gradio-client>=1.10.1",
    "httpx>=0.28.1",
    "lxml>=5.3.0",
    "pydantic>=2.11.5",
]
